require 'text'
describe Text do

    let(:time) { "11:30" }

    xit 'returns "order will arrive in 1 hour from now' do
      # subject.get_dishes
      # expect(subject.dishes).to eq data
    end

end