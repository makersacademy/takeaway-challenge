require 'confirm_message'

describe '#confirm_message' do

  it 'sends confirmation text' do
    expect(subject).to 
    expect { confirm_message }.not_to raise_error
  end

  expect(confirm_message).to
end
