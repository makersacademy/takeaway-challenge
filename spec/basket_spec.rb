require 'basket'

describe Basket do

  # it { is_expected.to respond_to(:summary) }

  # describe '#summary' do
  #   it 'displays the summary order so far' do
  #     order = Order.new
  #     expect(order.summary).to eq(sdfsfd)
  #   end
  # end

end
