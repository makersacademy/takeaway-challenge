require 'interface'

describe Interface do
  describe "#launch" do
    it 'stores prompted input' do
      # subject.launch
      # allow(STDIN).to receive(:gets) { "4\n" }
      # ordertime = (Time.now + 3600).strftime("%H:%M:%S")
      # expect(STDOUT).to receive(:puts).with("Thank you! Your order was placed and will be delivered before #{ordertime}")
    end
  end
end
