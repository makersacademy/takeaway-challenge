require 'sms'

describe Sms do
  it 'can create an instance of sms' do
    expect(subject).to be_kind_of(Sms)
  end
  # it 'can send the customer an order confirmation and expected time' do
  #
  # end
end
