require 'takeaway'

describe Takeaway do

  # subject(:takeaway) {described_class.new}
  # let(:menu) {double(:menu)}
  # let(:order) {double(:order)}

  # it 'should display the menu' do
  #   allow(menu).to receive(:options).and_return({'pizza' => 5.99, 'burger' => 4.99})
  #   expect(menu.read_menu).to include('pizza' => 5.99, 'burger' => 4.99)
  # end



end
