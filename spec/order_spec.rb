require 'order'

describe Order do

  it 'should take user input' do
  #  subject.input
  #  expect(itens.length).to be > 0
  end

  it 'should print the order taken' do
  #  menu = double('menu', :list => {:tofu => 3})
  #  subject.input
  #  expect {subject.total(menu)}.to output.to_stdout
  end

end
