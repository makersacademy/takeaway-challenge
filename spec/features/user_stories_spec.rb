require "takeaway"

describe "feature tests" do
	# As a customer
	# So that I can order the meal I want
	# I would like to be able to select some number of several available dishes
	#
# let(:takeaway) { double(TakeAway.new(menu), read_menu: shown_menu) }
# let(:menu) { double(:menu, show: shown_menu) }
# let(:shown_menu) { double("cheese cake: £5")}
#
# 	it "so I can order what I want, I would like to see a menu" do
# 		expect(takeaway.read_menu).not_to raise_error
# 	end

	# it "so I can select what I want, I would like to select from the menu" do
	# 	menu = Menu.new
	# 	expect(menu.selector).not_to raise_error
	# end

	# As a customer
	# So that I can verify that my order is correct
	# I would like to check that the total I have been given matches the sum of the various dishes in my order
	#
	# As a customer
	# So that I am reassured that my order will be delivered on time
	# I would like to receive a text such as "Thank you! Your order was placed and will be delivered before 18:52" after I have ordered




end
