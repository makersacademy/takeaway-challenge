require 'menu'

describe Menu do
  describe '#show' do
    it { is_expected.to respond_to(:show) }

    # it "should display the list of dishes with prices" do
    #   expect(subject.show).to 
    # end
  end
end
