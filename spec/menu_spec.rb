require "menu"

describe "Menu" do
describe "menu_options" do
  it "should return a hash of the items on the menu and their prices" do
    menu = Menu.new
    expect(menu.items_on_menu).to eq (menu.items_on_menu)
  end

end
end
