require 'coveralls'
Coveralls.wear!
require 'capybara/rspec'

feature "Restaurant can respond to incoming orders" do
  xscenario "can see when orders are placed" do
  end

  xscenario "can send confirmation text once order is placed" do
  end
end
