require './lib/confirmation_text.rb'

describe ConfirmationText do
  let(:text){ described_class.new }

    # it 'Should add 1 hour to time' do
    #   time = Time.now
    #   expect(text.time).to eq (Time.now + 3600)
    # end


end
