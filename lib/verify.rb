require './lib/order.rb'

class Verify

attr_reader :completed_order

# def initialize(order_klass: Order)
#   @current_order = []
#   @current_menu = []
#   @order_klass = order_klass
# end

def collate_order(current_order)

end
  # current_order[-1][:item_total] = (current_order[-1][item] \
  # * current_order[-1][:item_no])
  #
  # current_order[-1][:item_total] = (current_order[-1][item] \
  # * current_order[-1][:item_no])

end
