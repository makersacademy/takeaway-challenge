// this is me attempting to connect to database:
// var pg = require('pg');
// var connectionString = PG.connect(dbname: 'menu')
// var pgClient = new pg.Client(connectionString);
// pgClient.connect();
// var query = pgClient.query("SELECT * from menu;")
// query.on("row", function(row,result){
//   result.addRow(row);
// });
// request.map do |request|
  // Dish.new(id: request['id'], renter_name: request['renter_name'], room_name: request['room_name'], date: request['date'], request_status: request['request_status'],request_message: request['request_message'])

'menu.css'

var order = new Order([])
$('#add-to-order').on('click', function (){

})
